### v1.13.5.8 - 2026-08-25
-- WysiScan Extract Text (AI Prompt): renamed modal to "Extract Text From Image(s)"; results now render as per-entry label + value rows (Amazon Adds style) with Copy buttons; Media Description section is one editable block with "Copy Desc."; button text forced near-black so it stays readable in dark mode; button label shortened to "Run".
-- WysiScan Extract Text: modal is now draggable (drag header), resizable, and redraws content to fill the window both horizontally and vertically (flex-column layout). Added "Max" (maximize/restore) and "Open" (dumps full content to Notepad with focus) buttons.
-- WysiScan Extract Text: added a scan cache (last 3 scans, keyed by image filenames) in cli_cache.json; reopening with the same images auto-loads the prior results without re-running the AI. When loaded from cache the button shows "Re-Run" to force a fresh scan.
-- WysiScan Extract Text: API calls pinned to the absolute scanner URL (8010) with CORS enabled; the /run-cli-prompt route hardened to always return JSON (no more "Failed to fetch"), and the npx fallback removed.
-- WysiScan Extract Text: removed the crowding "read-only" note; the Basic / Detailed presets are now tagged "(read only)" in the dropdown instead.
-- Amazon Adds: Search Keywords no longer contain label / publisher / company names (Amazon forbids them) — Brand Name, Manufacturer and Series removed as keyword sources, plus a blocked-names set filters any label/publisher/company tokens.
-- Amazon Adds: the "PRODUCT_TAX_CODE" field label is now shown correctly as "Product Tax Code".
-- Amazon Adds: added a top "View" dropdown (Full / Small). Small shows only SKU, Your Price, Item Condition, Offer Condition Note, and Product Tax Code; switching back to Full restores the full layout.
-- docs/CONVENTIONS.md added as the durable home for UI/build/data conventions (modal draggable+resizable+redraw, pixel parity, scraping rules); referenced from AGENTS.md.
-- WysiScan UI: removed the blue "WysiScan" banner; added a left-aligned "☰ Menu" button (main-app styling) opening a drawer with Hot Keys / Theme / Exit only. AI Prompt stays on the main form.
-- WysiScan AI Prompt (Gemini) now reads its API key from the main app root .env (GEMINI_API_KEY) at scanner startup; falls back to a local .env for standalone use. Fixed loader so the key actually loads (resolves main root .env two dirs up from WysiScan).
-- WysiScan AI Prompt: added a "Prompt Preset" dropdown populated from prompts.json (Basic / Custom / Detailed). Selecting a preset fills the prompt text box; the text box is the single source sent to Gemini (no default_prompt.json duplication, which previously caused doubled output).
-- prompts.json simplified to plain instruction line-lists (stripped Step/a]/b] scaffolding) for cleaner AI input; "Custom" entry holds just GLOBAL RULE + FORMATTING as an editable base.
-- WysiScan AI Prompt: only the "Custom" preset is editable; Basic / Detailed (and future presets) load read-only and the Save button is disabled for them (enforced in UI and server-side).
-- WysiScan AI Prompt: added a "💾 Save" button that persists the current text-box content as the Custom prompt to prompts.json.
-- WysiScan autoscan fix: autoscan now triggers ONLY when Start Group is explicitly invoked (button or Enter). Refresh, rename, and group-name auto-load no longer fire an unwanted scan when autoscan is checked.
-- WysiScan & Amazon Adds: added an "Always on top" (in-page pin) toggle at the top of the AI Prompt modal and the Amazon Adds page so they float above other in-page content. (OS-level native-window topmost deferred to the standalone EXE build.)
-- Hot Keys: added editable bindings for "Amazon Adds" (default Ctrl+Shift+A) and "Discogs Adds" (default Ctrl+Shift+D). Both appear in the Hot Keys modal and are rebindable like the others.
-- Amazon Adds (Discogs API) overhaul: results now render as per-field rows with editable boxes and a Copy button each; Upload Photos / Merchant Shipping Group / Quantity rows kept alignment-matched but copy-less
-- Subject Keyword group: each keyword is its own deletable + editable row, with a "Copy Line" button that joins survivors with spaces
-- Keyword expansion: all lowercase, space-joined one-liner capped at 210 bytes, derived from the item's own data with an Amazon-safe blocklist (no "Check box: This product does not have a Product ID", promo/brand-only terms, etc.)
-- Gemini key/model pool: shared gemini_pool.py with a 6-key pool (keys.txt + root .env) and a curated models.txt; REST ?key= transport (works for both AIza and AQ keys) with per-combo failover ranked by test_results.json success rate. model_tester.py and WysiScan scanner_server.py now use the pool.
-- Discogs Adds (Phase 1): /api/adds/discogs now uses the Gemini pool to OCR scanned media images into {raw_text, tracks, credits}.
-- Discogs Adds (Phase 2): added parse_raw_text() that structures the loose OCR text into the fields the macro expects (artist, title, label, catalog, year, country, format, tracklist, credits, promo). discogs_add_macro now builds the 8 Discogs submission tables (Artists & Title, LCCN, BAOI, Format, Country & Release Year, Tracklist, Credits & Genres, Notes). Verified against the Lari White "Don't Fence Me In" scan set.
-- Discogs Adds page: Extract & Build now renders the 8 tables as editable per-field rows with Copy buttons (tracklist objects + array rows + header rows handled).
-- Discogs Adds: last successful extract is auto-saved to localStorage and auto-restored on page load / navigation-back, so results never go blank after a refresh; added a "Clear Saved" button to wipe it. Server /adds/discogs route now sends no-store cache headers so code fixes deploy without a hard refresh.
-- Discogs Adds popup fix: openAddsDiscogs() now reloads the existing popup window instead of just re-focusing it, so it always picks up the latest page/JS (a stale focused window was the cause of the blank-UI-after-extract symptom). Last extract is restored from localStorage on reload.
-- Discogs Adds: /api/adds/discogs now falls back to raw OCR text (parse_raw_text) when Gemini returns prose instead of JSON, so a failed JSON parse no longer returns a 422 — the 8 tables still build from the structured text.
-- Discogs Adds prompt fix: WysiScan/discogs_extract_prompt.txt now strictly forbids chain-of-thought / meta-commentary leaking into the raw_text field (examples added: "The user wants...", "Refining raw_text", "Final JSON"). Gemini had been returning its own narration as raw_text, which then polluted the release Title/Artist.
-- Discogs Adds fixes (verified against the live Gemini payload for Ruben Ramos "On The Prowl" REV-2003): (1) Artist override bug fixed -- the band-suffix regex was matching the bare label word "Revolution" (a standalone line near the barcode) as the artist, beating the real "Ruben Ramos And The Revolution". Now requires the "and <article> <noun>" structure (e.g. "and The Revolution"), so a lone "Revolution" label word is no longer mistaken for the artist. (2) Barcode (Text) now preserves the printed spacing -> "6 90647 20034 7" (was "690647200347"). (3) Genre/Style now resolves correctly (Latin / Tejano) -- the earlier Jazz/Pop Rock was a downstream effect of the wrong artist being sent to the Discogs lookup; fixing the artist fixed the genre. (4) main.py now loads Adds/discogs_add.py DIRECTLY from the source file path (importlib.util.spec_from_file_location) every request, bypassing __pycache__/.pyc entirely -- the stale-code trap that caused earlier blank-GUI / wrong-result regressions is now impossible. -- Discogs Add screen: Table 6 (Tracklist) now renders the track time in its own small editable text box (class .track-time, sized for 00:00, blank when none) to the right of the track title; the Copy button copies "Title  [time]".
-- Discogs Adds parser: label detection now catches regional label name lines like "DISCOS CBS INTERNATIONAL" (leading DISCOS/RECORDS/MUSIC/ENTERTAINMENT token), not just the hard-coded brand list. Verified: Label=DISCOS CBS INTERNATIONAL, Catalog=ZMC-80005, Format=Cassette on a real cassette OCR. (Note: evaluated three Gemini-provided rewrite attempts of discogs_add.py; all failed real OCR — missing Artist/Title, false-positive label from band-name substring, multiline catalog numbers, hardcoded Cassette fallback. Kept our verified parser.)
-- ChromeExt (browser extension, unpacked): added an auto-update mechanism. The installer now ships ChromeExt into C:\FYRTOOLS\WYSIWYG\ChromeExt and overwrites it on app upgrade (ignoreversion). background.js polls /api/extension-version every 5 min and auto-reloads (chrome.runtime.reload) when the version changes; added activeTab permission + version.json (v1.1). "Send to WYSIWYG Scraper" now also copies the Discogs link to the Windows clipboard.
-- Item Condition now derived from the Offer Condition Note as "Collectable - <lowest rating>" (Like New / Very Good / Good), with any +/- suffix stripped
-- Offer Condition Note captured verbatim from the Discogs marketplace listing comment
-- Your Price and SKU pulled from the actual marketplace listing
-- Country/Region of Origin forced to United States (our selling origin)
-- Discogs API token moved to .env (bundled with the EXE); no longer hardcoded in source
-- Drawer/menu icons resized to 13px to avoid a scrollbar
-- ChromeExt (browser extension, unpacked): added an auto-update mechanism. The installer now ships ChromeExt into C:\FYRTOOLS\WYSIWYG\ChromeExt and overwrites it on app upgrade (ignoreversion). background.js polls the app's new GET /api/extension-version every 5 min and calls chrome.runtime.reload() when the bundled version.json changes, so extension updates apply without a manual Reload on chrome://extensions. Extension version is stamped in ChromeExt/version.json (bump it on each extension change). manifest.json gained the "alarms" permission. One-time setup: point "Load unpacked" at C:\FYRTOOLS\WYSIWYG\ChromeExt on each machine.
-- ChromeExt: "Send to WYSIWYG Scraper" / "Send Page to WYSIWYG Scraper" now also copies the Discogs link to the Windows clipboard (via the service-worker clipboard API, with an active-tab inject fallback). Added the "activeTab" permission and bumped the extension to v1.1 (version.json + manifest.json) so the auto-update mechanism picks it up.

### v1.12.36.3 - 2026-08-19
-- All buttons moved to menu items in WYSIWYG and WysiScan
-- Added hot keys
-- Removed and oops save setting for non admin users
-- General GUI updates: removed footer, remove image
-- Removed market lookup tab
-- Re-added bug report/requests ability
-- Added Image reading AI for album text extraction

### v1.4.7.2 - 2026-02-02
-- Final release preparations: port reverted to 8008 and BETA identifiers removed from GUI.

### v1.2.1 - 2026-01-19 11:43
-- Fixed: 2X and 3X CD not showing properly
-- Fixed issue with radio buttons not clearing after new scrape or reset all

### v1.2.2 - 2026-01-19 12:21
-- a few GUI enhancements to dark mode colors
-- MEDIA CONDITION pull down always turn the MEDIA DESCRIPTION text box the same shade of red for ANY entries that do not include the word "FACTORY" i.e. factory sealed, factory new, etc.
-- Generate HTML now triggers on current scraped data when selecting DETAILS tab

### v1.2.4 - 2026-01-19 14:46
-- added Used Like New (NM-) to the turn DESC text box red.

### v1.2.4.1 - 2026-01-19 15:18
-- added a 4th decimal place to the version to account for minor build changes like adding options to the data.json file.

### v1.2.5.1 - 2026-01-19 15:42
-- added NM- to the change the DESC box read on SLEEVES

### v1.3.5.1 - 2026-01-19 17:28
-- made a weight/speed category and moved all weight/speed to this new field and changed location for color as well.

### v1.3.6.2 - 2026-01-20 11:24
-- fixed weight not showing with speed
-- fixed clearing of all previous scraped data when starting a new one
-- removed "import" from "worldwide" distribution

### v1.3.7.2 - 2026-01-20 15:01
-- DVD/DVD-Video bug fix

### v1.4.10.2 - 2026-02-20 09:46
* fixed issue where Artwork / Insert is missing on DVD/Blue ray

### v1.4.11.2 - 2026-02-20 09:50
* added check for cassette and blue ray box set sku builds

### v1.4.12.2 - 2026-02-20 10:19
* added check for cassette and blue ray box set sku builds
* changed multi media boxsets to be called MEDIABOX in the SKU
* removed space between disk count and media type for multi media box sets
* removed ALBUM from the desc of MEDIABOX sets
* removed duplicate BOX SET from MEDIABOX sets

### v1.4.14.2 - 2026-02-20 11:48
* reordering FORMAT TAGS so they are listed in order of appearance in multiselect box

### v1.4.15.2 - 2026-02-20 14:01
* fixed single format multiple count items from missing the media format in the description

### v1.5.15.2 - 2026-03-02 17:30
* added UberPaste and WysiScan to the main GUI

### v1.6.16.2 - 2026-03-18 12:14
* resolved an issue where the original scanned image were being cropped
* resolved an issue where the tilt correction was not auto applying
* added clear auto generated cropping square in manual crop
* added redo button to scanner

### v1.7.18.2 - 2026-03-31 16:32
* Added working WALMART screen
* bug squishing on WALMART screen

### v1.8.25.2 - 2026-04-14 16:33
Builder Tab:

1- https://www.discogs.com/sell/item/4108623660
   -ANV name: John "Jubu" Smith - Jubu
   -Display Name: Jubu
---FIXED---

2- URL: https://www.discogs.com/release/31834325-The-Hitman-Blues-Band-Hey-Can-You-Guys-Play?ev=
   Results: 'Hitman Blues Band, The - Hey Can You Guys Play…'
   Fix: Make sure 'The' is in the front
---FIXED---

3- URL: https://www.discogs.com/sell/item/4113078846
   Results: 
        "Used Like New Limited Edition Compilation 2000 USA Compilation, CD + NTSC DVD-Video Box Set is Out-of-Print with 14 Tracks - Volcano / Tool Dissectional 61422-31159-2 DVD"
    - Compilation is duplicated, make sure words like that only occur once in the generated DESCRIPTION
    - Number of Discs showing 3 when should only be TWO
          - Discosg Format data "CD, DVD, DVD-Video, NTSC, Compilation, Box Set, Limited Edition, Misprint"
          - DVD, DVD-Video are not two separate disks, so if it see's DVD and DVD-Video both then DVD-Video is only describing   
                the DVD. 
         - So in these cases DVD and DVD-Video (or DVD-AUDIO) are only one countable disk.
   - Misprint is also missing from the  generated DESCRIPTION on the BUILDER TAB
---FIXED---

Walmart Tab:

1- https://www.discogs.com/release/14566323-Gail-Jhonson-Pearls?ev=
    -Gail Johnson - Pearls - CD - Used Like New 2008 USA CD with 12 Tracks - nuGroove Records NUG-2002-2
    -Audio - CD
---FIXED---

2- Clear all fields not reloading new data from builder.
    Empty fields.
    Load from builder should over write data with fresh import from builder tab,
        not cached data like it does from the first run of the form..
--FIXED--

3- DVD should always be DVD-Audio
---FIXED---

4- Manufacturer Part Number:
    -ex: '/ / EV413027'
    -fix: no '/''s
    -corrected: EV413027
---FIXED---

5- Main Image URL & Additional Image URL (+) should auto paste
---FIXED---

6- If no 'part number' use the UPC/GTIN
---FIXED---

7- If no label name, use the artists name instead
---FIXED---

8- GTIN not populating right 13+ digits is GTIN
---FIXED---

9- Site Start Date starts moving to next day after 6pm. Make sure time zone is CST
---FIXED---

10- USE API option:  https://www.discogs.com/sell/item/4107960387
     -SKU / Location
---FIXED---

11- Country is coming up as Christian Country, and POP is showing up as BRITISH POP
---FIXED---

12- Country of origin is not populating correctly
---FIXED---

### v1.8.26.2 - 2026-05-04 09:40
* Fixed Discog's URL change from sell to shop

### v1.9.27.2 - 2026-05-06 13:40
* added multiselect menus for conditions
* added edit ability on admin screen for conditions

### v1.9.27.3 - 2026-05-06 16:16
* consolidated condition menu items

### v1.9.28.3 - 2026-05-11 11:16
* comma removal
* fix sleeve pack and condition misplacement bug
* fix import being added to USA & Europe
* fixed All Media bug with sleeve pack.
* corrected spelling of 'Bluray'
* removed second deluxe from desc

### v1.9.28.4 - 2026-05-12 15:32
-- Fixed: The word 'with' was being omitted before the sleeve description/flaws when a sleeve condition was specified.